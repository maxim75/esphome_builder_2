esphome:
  name: "office-speaker"
  friendly_name: "office-speaker"

  # on_boot:
  #   priority: -100
  #   then:
  #       homeassistant.event:
  #         event: esphome.top_steps_radar_movement
  #         data:
  #           message: top steps radar movement

      # homeassistant.service:
      #   service: input_boolean.turn_on
      #   data:
      #     entity_id: input_boolean.top_steps_radar_movement
      # - input_boolean.set: input_boolean.top_steps_radar_movement

esp32:
  board: esp32dev
  framework:
    type: arduino

# Enable logging
logger:

# Enable Home Assistant API
api:
  encryption:
    key: "C8ZbF6V/mPSQX/hH/D/qZx/b5rTC4dpbiD0fWvf7yFQ="

ota:
  password: "bab7549b3ba5c594ca066d2ed65e3b21"
  platform: esphome

wifi:
  ssid: !secret wifi_ssid
  password: !secret wifi_password

  # Enable fallback hotspot (captive portal) in case wifi connection fails
  ap:
    ssid: "Office Speaker Fallback Hotspot"
    password: "ks5tybCfmy6g"

captive_portal:

# remote_transmitter:
#   pin: 26
#   carrier_duty_percent: 100%

# binary_sensor:
#   - platform: gpio
#     pin: 25
#     name: "PIR Sensor"
#     device_class: motion
#     on_state:
#       # - homeassistant.event:
#       #     event: esphome.top_steps_radar_movement
#       #     data:
#       #       message: top steps radar movement

#       - remote_transmitter.transmit_rc_switch_raw:
#           code: '100010000000000010111110'
#           protocol: 2
#           repeat:
#             times: 10
#             wait_time: 50ms

#     #   - delay: 10s
#     #   - deep_sleep.enter



# deep_sleep:
#   run_duration: 20s
#   sleep_duration: 10min
#   wakeup_pin: 25


# esp32_ble_tracker:
#   on_ble_advertise:
#     - mac_address:
#         - "3C:22:FB:CF:F4:AD"
#         - "00:1B:66:0E:1E:D6"
#         - "C5:8E:92:6C:3B:39"
#       then:
#         - lambda: |-
#             ESP_LOGD("ble_adv", "New BLE device");
#             ESP_LOGD("ble_adv", "  address: %s", x.address_str().c_str());
#             ESP_LOGD("ble_adv", "  name: %s", x.get_name().c_str());
#             ESP_LOGD("ble_adv", "  Advertised service UUIDs:");
#             for (auto uuid : x.get_service_uuids()) {
#                 ESP_LOGD("ble_adv", "    - %s", uuid.to_string().c_str());
#             }
#             ESP_LOGD("ble_adv", "  Advertised service data:");
#             for (auto data : x.get_service_datas()) {
#                 ESP_LOGD("ble_adv", "    - %s: (length %i)", data.uuid.to_string().c_str(), data.data.size());
#             }
#             ESP_LOGD("ble_adv", "  Advertised manufacturer data:");
#             for (auto data : x.get_manufacturer_datas()) {
#                 ESP_LOGD("ble_adv", "    - %s: (length %i)", data.uuid.to_string().c_str(), data.data.size());
#             }


# spi:
#   clk_pin: 14
#   mosi_pin: 12

# display:
#   - platform: max7219
#     cs_pin: 13
#     num_chips: 1
#     lambda: |-
#       it.print("01234567");




    
# After
i2s_audio:
  i2s_lrclk_pin: GPIO26
  i2s_bclk_pin: GPIO25

media_player:
  - platform: i2s_audio
    name: ESPHome I2S Media Player
    dac_type: external
    i2s_dout_pin: GPIO33
    mode: mono

binary_sensor:
  - platform: status
    name: "Status"