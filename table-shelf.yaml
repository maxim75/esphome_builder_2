esphome:
  name: table-shelf
  friendly_name: table-shelf

esp32:
  board: esp32-s3-devkitc-1
  framework:
    type: esp-idf
    version: dev
    sdkconfig_options:
      CONFIG_ESP32S3_DATA_CACHE_64KB: "y"
      CONFIG_ESP32S3_DATA_CACHE_LINE_64B: "y"
      CONFIG_ESP32S3_INSTRUCTION_CACHE_32KB: "y"

      # Moves instructions and read only data from flash into PSRAM on boot.
      # Both enabled allows instructions to execute while a flash operation is in progress without needing to be placed in IRAM.
      # Considerably speeds up mWW at the cost of using more PSRAM.
      CONFIG_SPIRAM_RODATA: "y"
      CONFIG_SPIRAM_FETCH_INSTRUCTIONS: "y"

      CONFIG_BT_ALLOCATION_FROM_SPIRAM_FIRST: "y"
      CONFIG_BT_BLE_DYNAMIC_ENV_MEMORY: "y"

      CONFIG_MBEDTLS_EXTERNAL_MEM_ALLOC: "y"
      CONFIG_MBEDTLS_SSL_PROTO_TLS1_3: "y"  # TLS1.3 support isn't enabled by default in IDF 5.1.5

# Enable logging
logger:

# Enable Home Assistant API
api:
  encryption:
    key: "wcxkw93rY1jDfEEFt3AIF6kIHDKLj773JpNnbHrwTds="

ota:
  - platform: esphome
    password: "afe9ddfd34fe4f39835f464c81d00c47"

# wifi:
#   ssid: !secret wifi_ssid
#   password: !secret wifi_password

#   # Enable fallback hotspot (captive portal) in case wifi connection fails
#   ap:
#     ssid: "Table-Shelf Fallback Hotspot"
#     password: "AfkRgxpoYhLn"

# captive_portal:

web_server:
  port: 80

switch:
  - platform: gpio
    name: "Relay"
    id: relay_1
    pin: 17

  - platform: gpio
    name: "Office media player"
    id: office_media_player
    pin: 16

# Example configuration entry
output:
  - platform: ledc
    pin: 6
    id: gpio_6

  - platform: ledc
    pin: 7
    id: gpio_7

  - platform: ledc
    pin: 14
    id: gpio_14

  - platform: ledc
    pin: 15
    id: gpio_15

# Example usage in a light
light:
  - platform: monochromatic
    output: gpio_6
    name: "Office shelf"

  - platform: monochromatic
    output: gpio_7
    id: office_chair
    name: "Office chair"

  - platform: monochromatic
    output: gpio_14
    name: "PWM 3"

  - platform: monochromatic
    output: gpio_15
    id: office_table_lamp
    name: "Office table lamp"

ethernet:
  type: W5500
  clk_pin: 12
  mosi_pin: 11
  miso_pin: 13
  cs_pin: 2
  interrupt_pin: 5
  reset_pin: 1
